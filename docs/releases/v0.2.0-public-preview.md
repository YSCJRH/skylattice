# v0.2.0 Public Preview

This file is the tracked release draft for the first public preview. It is designed to make the GitHub release page, repository description, and README tell the same story.

## Release Positioning

Recommended GitHub repository description:

> Local-first AI agent runtime for persistent memory, governed repo tasks, and Git-native self-improvement.

Recommended repository topics:

- `local-first`
- `ai-agent`
- `agent-framework`
- `memory`
- `sqlite`
- `cli`
- `fastapi`
- `developer-tools`
- `github`
- `personal-agent`

## What This Release Is

Skylattice is an early public preview of a local-first agent runtime that keeps:

- durable memory in local SQLite-backed state
- operator-facing behavior in tracked docs, prompts, configs, and ADRs
- repo work behind explicit approval and validation boundaries
- bounded self-improvement behind reviewable Git paths and rollback metadata

## What Works Today

- `skylattice doctor` for zero-credential runtime verification
- governed `task-agent` runs with deterministic edit modes and run inspection
- `technology-radar` runs with GitHub discovery, evidence capture, bounded experiments, promotion logs, and rollback metadata
- Windows-first CI using the same tracked validation commands as the runtime
- public-safe sample outputs and walkthroughs under `examples/redacted/`

## 5-Minute Verification Path

1. `python -m pip install -e .[dev]`
2. `python -m skylattice.cli doctor`
3. `python -m pytest -q`
4. `python tools/run_validation_suite.py`
5. compare local results with `examples/redacted/doctor-output.json`

## Token-Enabled Workflows

Task path:

```bash
skylattice task run --goal "Refresh README and prepare a draft PR" --allow repo-write --allow external-write
```

Radar path:

```bash
skylattice radar scan --window weekly --limit 20
```

Expected artifacts to inspect:

- `examples/redacted/task-run-sample.md`
- `examples/redacted/task-run-sample.json`
- `examples/redacted/radar-sample.md`
- `examples/redacted/radar-run-sample.json`

## Visual And Proof Assets

- `docs/assets/doctor-snapshot.svg`
- `docs/assets/task-run-snapshot.svg`
- `docs/assets/runtime-architecture.svg`

## Known Limits

- task execution is text-native and does not perform AST-aware refactors
- meaningful task planning still depends on `OPENAI_API_KEY`
- radar discovery depends on `GITHUB_TOKEN`
- automatic promotion remains constrained to whitelisted tracked paths
- the project is not yet packaged as a hosted service or broad integration framework

## Validation Baseline

- `python -m pytest -q`
- `python -m compileall src/skylattice`
- `python -m skylattice.cli doctor`
- `python tools/run_validation_suite.py`

The command allowlist lives in `configs/task/validation.yaml`, and CI reads the same policy through `tools/run_validation_suite.py`.

## Recommended GitHub Release Body Shape

- what Skylattice is
- what works today
- 5-minute verification path
- screenshots and sample outputs
- known limits
- validation commands
- roadmap pointer
- feedback wanted

## Feedback Wanted

After publishing the release, collect three kinds of feedback first:

- cold-visitor clarity: did the first screen explain why the repo matters?
- first-run confidence: could someone verify the project without tokens?
- recommendation gap: what still makes the repo feel early or risky?

## Next Public Steps

- apply the repository description and topics above in GitHub settings
- publish this release text as the first GitHub release body
- attach the SVG proof assets or link to them from the release notes
- open a small set of public roadmap and feedback issues after release
