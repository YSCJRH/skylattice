# Task Brief: v0.3.1 Release And Phase 5 Closeout

## Intent

- publish a stable patch release that packages the current Phase 5 operational-closure work as the new latest stable signal
- success means version metadata, release notes, Pages surfaces, public-readiness tests, and the GitHub release all point at the same `v0.3.1 Stable` story

## Constraints

- tracked artifacts to edit: version metadata, release docs, README and Pages references, AI-distribution surfaces, public-readiness tests, and this task brief
- local-only artifacts expected to change: optional release-body staging file under `.local/work/` for GitHub release publishing
- explicit non-goals: no new runtime APIs, no new provider, no governance widening, no resident scheduler, no autonomy expansion

## Affected Subsystems

- package and citation metadata
- public Pages and release surfaces
- AI-distribution and outreach references
- public-readiness verification and release publishing

## Verification

- `python -m pytest -q`
- `python -m compileall src/skylattice`
- `python -m skylattice.cli doctor`
- `python tools/run_validation_suite.py`
- `python -m mkdocs build --strict`
- `git diff --check`
- manual checks for `gh release view`, Pages/latest-release references, and current `main` state

## Notes

- default release assumption: `v0.3.1 Stable` as a patch follow-up to `v0.3.0 Stable`
- release body should describe Phase 5 operational closure, repeated weekly validation proof, prompt truth-source alignment, and honest auth-dependent validation reporting
- `v0.3.0 Stable` remains a historical stable milestone and should stay linked in the release history
