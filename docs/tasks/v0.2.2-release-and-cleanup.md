# Task Brief

## Intent

- return `main` to the repository root and clean up merged topic branches
- package the external authority kit as `v0.2.2 Stable` without widening runtime behavior

## Constraints

- tracked artifacts to edit: version metadata, release docs, Pages navigation, AI distribution surfaces, public-readiness tests
- local-only artifacts expected to change: none after cleanup; the private ChatGPT benchmark remains on a local-only branch
- explicit non-goals: do not merge the private benchmark harness, do not add runtime APIs, do not widen governance or autonomy scope

## Affected Subsystems

- kernel metadata
- public Pages and release surfaces
- outreach and discoverability operations
- public-readiness verification

## Verification

- `python -m pytest -q`
- `python -m compileall src/skylattice`
- `python -m skylattice.cli doctor`
- `python -m mkdocs build --strict`
- manual checks for branch/worktree cleanup and GitHub release metadata

## Notes

- `v0.2.2` is a stable follow-up to `v0.2.1`, focused on the authority-building layer
- keep issue `#2` and `#3` open; rename issue `#4` to match the stable release context